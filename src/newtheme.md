# CODEX_PROMPT.md — Implement Template 02 (Tide / Menthe) in Navera

## Objective
Implement the provided Template 02 ("Tide — Menthe") as a new UI theme inside the existing Navera app on att_v14.tsx.

This is a **design-only transformation**.

🚫 Do NOT change:
- functionality
- routing
- Supabase queries
- authentication
- state management
- data structures

---

## Source Template
Use the provided HTML/CSS template as the **visual reference**.
The source template is newtheme.html

Important:
- Do NOT copy it as raw HTML into the app
- Convert it into **React + Tailwind (or existing styling system)**
- Reuse existing components where possible

---

## Implementation Strategy

### 1. Replace the current UI theme

Replace the current visual styling with Template 02 ("Tide — Menthe").

Do not add a theme toggle.

Do not keep the old theme as an option.

The app should use this new visual template everywhere by default.